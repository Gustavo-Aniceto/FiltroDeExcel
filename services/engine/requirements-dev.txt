# Dependencias apenas de desenvolvimento e teste.
#
# Separadas de requirements.txt de proposito: a imagem Docker instala apenas o
# runtime, e nao carrega ferramenta de teste em producao. Quem desenvolve na
# maquina recebe as duas listas (ver scripts/setup.mjs).
-r requirements.txt

pytest>=8.3.0
httpx>=0.28.0
ruff>=0.9.0
